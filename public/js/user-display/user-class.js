export default class User {
    constructor(user_id, username, email, profile_image) {
        this.user_id = user_id;
        this.username = username;
        this.email = email;
        this.profile_image = profile_image
    }
}